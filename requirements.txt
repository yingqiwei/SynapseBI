# ============================================================
# SynapseBI — Python 依赖
# ============================================================
# 安装:
#   pip install -r requirements.txt
#
# 可选分组:
#   pip install -r requirements.txt           # 核心依赖
#   pip install ".[pdf-full]"                 # 含 PDF OCR
#   pip install ".[dev]"                      # 开发工具
# ============================================================

# --- 核心 ---
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0
tabulate>=0.9.0

# --- Web 框架 ---
fastapi>=0.109.0
uvicorn[standard]>=0.25.0
pydantic>=2.0.0
python-multipart>=0.0.6

# --- Streamlit 前端 ---
streamlit>=1.31.0

# --- 向量数据库 ---
qdrant-client>=1.7.0
sentence-transformers>=2.5.0

# --- PDF 解析 ---
PyMuPDF>=1.23.0

# --- 可选 PDF 后端 ---
# unstructured[pdf]>=0.12.0      # Unstructured 版面分析
# paddlepaddle>=3.0.0            # PaddlePaddle (CPU)
# paddleocr>=2.7.0               # PaddleOCR

# --- Excel ---
openpyxl>=3.1.0
xlrd>=2.0.0

# --- 图表 ---
matplotlib>=3.8.0
plotly>=5.18.0

# --- LLM 客户端 ---
openai>=1.6.0
# anthropic>=0.18.0              # 使用 Anthropic Claude 时取消注释
requests>=2.31.0

# --- Docker 相关（生产） ---
# docker>=7.0.0

# --- 开发 ---
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# black>=24.0.0
# ruff>=0.2.0
# mypy>=1.8.0
